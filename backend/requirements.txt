# FLUX Backend — Python dependencies
# Run:  pip install -r backend/requirements.txt

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0

# ── Market data ───────────────────────────────────────────────────────────────
yfinance>=0.2.40

# ── Persistence ───────────────────────────────────────────────────────────────
aiosqlite>=0.20.0           # async SQLite driver
pymysql>=1.1.0              # MySQL driver for the seeded /db/* dataset

# ── Scheduling ────────────────────────────────────────────────────────────────
apscheduler>=3.10.4         # AsyncIOScheduler

# ── RAG / Vector DB ───────────────────────────────────────────────────────────
# chromadb ships with the ONNX all-MiniLM-L6-v2 embedding model (~80 MB DL on first use)
chromadb>=0.5.23

# ── Prediction agent ──────────────────────────────────────────────────────────
# Indicators + fractional differentiation are implemented in pure pandas/numpy
# (pandas-ta is intentionally avoided — it imports numpy.NaN, removed in numpy 2.x).
pandas>=2.2
numpy>=1.26
scikit-learn>=1.4          # models, calibration, metrics
statsmodels>=0.14          # ADF stationarity test (frac-diff order) + ARIMA baseline
xgboost>=2.0               # direction classifier (Layer 2a)
transformers>=4.40         # FinBERT news sentiment (Layer 2c)
torch>=2.2                 # FinBERT backend (CPU or CUDA)
hmmlearn>=0.3              # market regime detection (Part 4)
scipy>=1.11                # forward-filter logsumexp
arch>=6.3                  # GARCH(1,1) volatility → shapes the conformal band (Phase 4)
# Conformal bands are implemented in-house (conformal.py) on our own purged splitter rather
# than via MAPIE, whose internal CV isn't purged and would leak on overlapping labels.
chronos-forecasting>=2.2   # zero-shot foundation-model baseline to beat (Phase 8, baselines.py)
praw>=7.7                  # Reddit retail-flow sentiment (Phase 13; needs REDDIT_* keys)
pytest>=8.0                # test suite (test_prediction.py, test_features.py)
# Optional (graceful no-op without keys): FRED macro (FRED_API_KEY), Alpaca paper (ALPACA_* via
# httpx — no SDK needed). torch (already present) powers the LSTM magnitude head (magnitude.py).
